package com.swd.exe.teammanagement.enums.notification;

public enum NotificationType {
    JOIN_REQUEST,
    VOTE,
    POST,
    IDEA,
    SYSTEM,
    JOIN_ACCEPTED,
    JOIN_REJECTED,
    INVITE,
    INVITE_ACCEPTED,
    INVITE_DECLINED

}
