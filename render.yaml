services:
  - type: web
    name: exe-team-management
    env: java
    region: oregon
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -Dserver.port=$PORT -jar target/team-management-0.0.1-SNAPSHOT.jar
    # Environment variables sẽ được set trực tiếp trên Render Dashboard
    # Không để thông tin nhạy cảm trong file này vì sẽ bị public trên Git